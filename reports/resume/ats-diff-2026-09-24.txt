Resume ATS/consistency check — 2026-09-24 (final)
Session: refresh-2026-09 wave, Step 4 (PR #225, branch fix/resume-oss-and-paper-status)

STATUS: DONE. public/resume.pdf was regenerated from the (rewritten) private
.docx master via LibreOffice 26.8.0.3 headless, and matches
reports/resume/approved-2026-09-24.txt exactly (whitespace-normalized).

=====================================================================
0. Stale-master finding (why this needed a docx rewrite, not just an edit)
=====================================================================
.assets/resume-sources/Gaurav_Gandhi_Resume_2026.docx (gitignored, private)
was a materially older resume revision: 6 of its 47 paragraphs matched the
approved content at all (all 6 bare section headers); title, role count (2
vs the approved 3), section names, and every bullet's wording differed. It
had not been kept in sync with public/resume.pdf since at least PR #225's
own commit (48233ac, 2026-09-23), which found the same staleness and worked
around it with direct PDF text surgery instead of touching the docx. Per
GG's explicit instruction this session, the docx was rewritten in place
(python-docx, reusing the document's own existing paragraph/run styles via
XML clone rather than inventing new formatting) so it now equals
approved-2026-09-24.txt line-for-line, and re-exported — the private
master and the public artifact are back in sync. Backup taken first:
.assets/resume-sources/Gaurav_Gandhi_Resume_2026.docx.bak-step4-2026-09-24
(other .bak-* files left untouched).

=====================================================================
1. Toolchain
=====================================================================
LibreOffice 26.8.0.3, extracted via lessmsi (run from PowerShell — Git Bash
mangled the Windows args in earlier attempts, see prior session notes) into
a portable, non-system location. Invoked headless with a fresh, isolated
profile (-env:UserInstallation). Fidelity check first: exported the
ORIGINAL (still-stale) docx through this same toolchain before touching
anything — 2 pages, all Calibri fonts fully embedded, confirming the
pipeline itself introduces no page-count or font-embedding regressions
before it was trusted with the real rewrite.

=====================================================================
2. Docx rewrite — text fidelity
=====================================================================
docx paragraph text (python-docx .text, tabs normalized to spaces — tabs
are intentional right-alignment devices reused from the original document's
own paragraphs, not content) vs reports/resume/approved-2026-09-24.txt:
49 paragraphs, 49 approved lines, 0 mismatches.

Hyperlinks preserved/added, sourced to public/resume.pdf's own /Annots URIs
(pypdf), reusing python-docx's relationship mechanism (some reused the
docx's own pre-existing relationship IDs when the URL was already declared,
e.g. the GitHub profile link and 3 of the 5 project Code/Live links; the
rest newly created):
  header: LinkedIn, GitHub, Portfolio, Hugging Face (4)
  projects: Live + Code for TriageIQ, Style Maitri, Samidha Reviews
            (ReviewIQ), AgentGauge, tracegauge (10)
  projects intro: "gaurav-gandhi.vercel.app" (1)
  total: 15 hyperlinks, all verified against the shipped PDF's own
  annotation rects (page/x-position) before being assigned to the right
  project/label.

=====================================================================
3. Export — page count, fonts, size
=====================================================================
  page count: 2 (unchanged from main's shipped PDF — verified via pypdf,
  independently re-derived, not cited from a prior claim)
  file size: 139,912 bytes (old shipped: 143,658 bytes; docx-source fidelity
  check on the ORIGINAL stale docx: 158,607 bytes — all in the same range,
  no bloat or truncation)

  Fonts (pypdf, walking /Resources /Font -> /FontDescriptor ->
  FontFile/FontFile2/FontFile3), identical set on both pages:
    /F1  TrueType  BAAAAA+Calibri-Bold        embedded=True
    /F2  TrueType  CAAAAA+Calibri             embedded=True
    /F3  TrueType  DAAAAA+Calibri-Bold        embedded=True
    /F4  TrueType  EAAAAA+Calibri-Bold        embedded=True
    /F5  TrueType  FAAAAA+Calibri-Bold        embedded=True
    /F6  TrueType  GAAAAA+Calibri-BoldItalic  embedded=True
    /F7  TrueType  HAAAAA+Calibri-Italic      embedded=True
    /F8  TrueType  IAAAAA+Calibri             embedded=True
  All 8 font variants fully embedded (subsetted, "+" prefix = standard PDF
  subset naming). Real Calibri, not a Carlito substitution — this machine's
  LibreOffice found genuine Calibri (from the OS/Office font set) rather
  than falling back to its metric-compatible sibling. No font substitution,
  no missing glyphs.

=====================================================================
4. ATS check — pdftotext (both modes) vs approved.txt
=====================================================================
  pdftotext -enc UTF-8 (no -layout): 8,567 chars — U+FFFD=0, fi-ligature=0,
    fl-ligature=0, private-use-area chars=0
  pdftotext -enc UTF-8 -layout:      9,174 chars — U+FFFD=0, fi-ligature=0,
    fl-ligature=0, private-use-area chars=0

  Reading-order reconstruction (pdfplumber per-line x0/top coordinates,
  same deterministic bullet/continuation-merge method used to build
  approved.txt itself) -> 49 logical lines, whitespace-normalized diff
  against reports/resume/approved-2026-09-24.txt:

    0 lines differ (of 49). Reading order matches section-for-section
    (header -> summary -> experience [3 roles, correct order] -> projects
    [5, correct order] -> research -> skills -> education -> certs).

  One cosmetic, expected artifact: this LibreOffice build's Calibri
  hyphenation wraps "github.com/gaurav-gandhi-2411" as "github-" /
  "gandhi-2411" across the header's line-wrap (vs. the old PDF's renderer,
  which wrapped without a mid-word hyphen). Text-content-equal once
  merged; not a garbling or drift, just a different line-break point from
  a different font-shaping engine version. Called out explicitly rather
  than silently absorbed into the "0 differ" count.

=====================================================================
5. content/resume-data.json / content/resume-metrics.json
=====================================================================
Updated only the Samidha Reviews / ReviewIQ project entry (the only place
the stale 83.8%/83% figure lived) to 78.6% / 43-review CI-gated eval /
76% threshold, sourced to review-iq/eval/results.json (git_sha 5c5c8e0,
generated_at 2026-09-19T21:59:11Z, overall_score 0.7859851844649027, n=43,
threshold 0.76). resume-metrics.json's resume_pdf_sha256 and synced_at
bumped to match the newly exported PDF
(164b3290f0734a1169543f17ebfe90c2504ac16b1b9c69dd6e2598f50c3a0304,
2026-09-24), and its reviewiq:extraction-eval claim updated to the same
78.6%/43/76% figures.

`node scripts/check-resume-pdf-consistency.mjs`: PASS — "14 projects in
resume-data.json, 5 covered by public/resume.pdf and checked (expected 5).
checked: agentgauge, reviewiq, style-maitri, tracegauge, triageiq. OK — no
public/resume.pdf vs. content/resume-data.json contradictions found."

=====================================================================
6. e2e
=====================================================================
`npx playwright test e2e/resume.spec.ts`: 4/4 passed.
`npx playwright test` (full suite, per this repo's own CLAUDE.md rule —
content edits can break specs in unrelated files): 496 passed, 1 failed,
109 skipped. The 1 failure (`hero-socials.spec.ts`, mobile, tooltip-opacity
CSS timing) is unrelated to any resume/label content — re-ran it in
isolation and it passed cleanly (4/4), confirming a parallel-run timing
flake, not a regression from this change.

chatbot index: not rebuilt. `scripts/chatbot/build-index.mjs` reads
content/case-studies, products.ts, provenance.md, experience.ts,
availability.ts, site.ts — it does not ingest resume-data.json,
resume-metrics.json, or public/resume.pdf, so this change has no effect on
it (verified by reading the script, not assumed).

=====================================================================
6b. D1 working-paper wording — cross-surface grep
=====================================================================
Orchestrator-requested check: grep -i for "under submission", "preprint",
"arxiv" across every surface this PR touches. Zero hits everywhere:
  content/resume-data.json                          : 0 hits
  reports/resume/approved-2026-09-24.txt             : 0 hits
  .assets/resume-sources/Gaurav_Gandhi_Resume_2026.docx (python-docx text) : 0 hits
  new PDF text, pdftotext (no -layout)               : 0 hits
  new PDF text, pdftotext -layout                    : 0 hits
RESEARCH section reads, consistently, everywhere: "Two first-author
working papers (drafts, not yet submitted):" — D1's wording, not
"under submission" and not "preprint (arXiv ID pending)". (Main still
carries the old wording as of this writing, per the orchestrator's note —
that's PR #225's own unmerged D1 fix (commit 48233ac), not something this
session needed to touch; this session's job was only to confirm the
branch's docx/PDF/JSON stayed consistent with it while resyncing the
docx master, which they do.)

=====================================================================
7. Screenshots
=====================================================================
reports/screenshots/resume-step4/{old,new}-p{1,2}.png — rendered via this
same LibreOffice toolchain (soffice --convert-to png on a single-page-split
PDF, since multi-page PDF->PNG only renders page 1 by default). Text
content is identical between old and new (per section 4); visual styling
differs because the docx's own native run styles were reused as instructed
rather than reproducing the old PDF's specific choices — the new PDF uses
the docx's navy-blue (#1F4E79) heading/name color and italic role headers,
where the old PDF used plain black text with underlined links and a thin
horizontal rule under each section heading. Both are legitimate, readable,
2-page layouts; the difference is a deliberate consequence of "reuse the
document's existing styles" rather than a bug. Embedded inline in PR
#225's body via raw.githubusercontent.com.
