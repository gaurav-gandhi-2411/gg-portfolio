## 6. Cross-surface consistency

Scope: independent re-verification of spec findings F1–F15 plus a fresh hunt for new
conflicts, using only free/zero-cost reads. `ANTHROPIC_API_KEY` was not touched by any
command in this section.

**Sources fetched fresh (2026-09-23, this session, all commands re-run just now — none of
the spec's 2026-09-23 numbers were assumed to still hold):**

- Site home: `curl -s https://gaurav-gandhi.vercel.app/` → HTTP 200, 332,291 bytes.
- Site `/projects`: `curl -s https://gaurav-gandhi.vercel.app/projects` → HTTP 200, 166,441 bytes.
- Site `/resume.pdf`: `curl -s https://gaurav-gandhi.vercel.app/resume.pdf` → HTTP 200, 83,390 bytes, converted with `pdftotext -layout`.
- README: `gh api repos/gaurav-gandhi-2411/gaurav-gandhi-2411/readme --jq .content` (base64-decoded).
- GH profile: `gh api users/gaurav-gandhi-2411 --jq '{bio,name,blog,company,email}'`.
- GH issues/PRs: `gh issue list` / `gh pr list --repo gaurav-gandhi-2411/gg-portfolio --state open --json ...`.
- PyPI ground truth: `curl -s https://pypi.org/pypi/<pkg>/json` for all three packages (cross-checked against section 5's independent PyPI pull; both sessions agree exactly).
- Upstream OSS check: `gh search prs --author gaurav-gandhi-2411 --repo google/adk-python` + `gh pr view <n> --repo google/adk-python --json mergedAt,state` for every result.

**Limitation (stated per task instructions, not guessed around):** the site is Next.js;
`curl` on `/` and `/projects` returned a large, fully server-rendered HTML payload (300k+
and 160k+ bytes respectively) that includes both the rendered DOM *and* an embedded RSC
JSON payload with the same content duplicated as data — so for this site, unlike a typical
client-shell SPA, `curl` reliably captured real content, not an empty shell. This was
verified by reading actual card text, headings, and figures out of the response, not
assumed. No page required a headless browser to extract the values below.

### 6.1 Re-verified: spec findings F1–F15

| # | Surface(s) | Finding | Site value | README value | Bio value | Status |
|---|---|---|---|---|---|---|
| F1 | GH bio vs. site vs. README | Title mismatch | "Lead Data Scientist" (title tag, JSON-LD `jobTitle`, hero byline, experience-ladder current rung "Lead Data Scientist · Aug 2026 – Present") — verified, matches resume PDF p.1 exactly | "Lead Data Scientist" (banner alt text, opening line) | **"Senior Data Scientist"** (`gh api users/gaurav-gandhi-2411 --jq .bio`) | **Still open.** Bio is the one stale surface; site and README agree and match the resume. |
| F2 | GH README vs. site vs. PyPI | adk-tracegauge version/release count | "v0.9.1 · 17 releases" (live `data-live-value` span) | "8 releases on PyPI, current v0.4.1" | n/a | **Still open.** PyPI ground truth (`curl pypi.org/pypi/adk-tracegauge/json`) = `0.9.1`, 17 releases — matches site exactly, README is stale by 9 releases and 5 minor versions. See §5.2 for full release list. |
| F3 | Both | Gold Rate Tracker backtest fold count | "199-fold backtest" (project card, both `/` and `/projects`) | "204-fold backtest" | n/a | **Still open.** No change on either surface since spec baseline. |
| F4 | Both | Gold Rate Tracker wording | Card: "predicts tomorrow's [rate], shipping the plain baseline because the model I trained never beat it" | GH repo description (`gh api repos/.../gold-rate-tracker --jq .description`): "honest flat-hold range (**no fake predictions**)" | n/a | **Still open, wording conflict confirmed as-is.** The two aren't strictly contradictory (site clarifies it ships the naive baseline, not the trained model) but "predicts tomorrow's" vs. "no fake predictions" reads as opposed on a skim — a recruiter would need to read both fully to reconcile them. |
| F5 | Both | Paper status | Both papers: "Preprint, pending arXiv" (project card + citation panel) | Both papers: "Under submission" (Research table `Status` column, both rows) | n/a | **Still open.** D1 (spec §7) is still unresolved — this needs a GG ruling, not a mechanical fix. |
| F6 | GH README | Journey row contradicts current role | Experience section: only entry is "Uber Technologies, Uber AI · Jun 2024 – Present · via Indium Software," internal ladder Data Scientist → Senior Data Scientist → **Lead Data Scientist (current)** | Journey table's last row: `2025–now \| Leading a 5-person GenAI team... \| **Independent**` — directly under the banner's own opening line "Lead Data Scientist building production GenAI systems in **Uber's AI org** (via Indium Software)" | n/a | **Still open.** The contradiction is internal to the README (its own banner vs. its own Journey table), not just README-vs-site. |
| F7 | GH README vs. site vs. PyPI | tracegauge (PyPI) coverage | Site's tracegauge card correctly links Source → `github.com/gaurav-gandhi-2411/token-efficiency-scorer`, `pip install tracegauge`, live v0.13.0 · 19 releases | `tracegauge` is **not mentioned anywhere** in the README (only `adk-tracegauge` and `agentgauge-harness` appear in the Shipped table) | n/a | **Still open** (README-omission half). **Correction to the spec's own cross-reference:** PyPI's `tracegauge` package `project_urls.Repository` field itself declares `token-efficiency-scorer` as its source repo — the site is not guessing or drifting, it is reading PyPI's own metadata correctly. Separately, issue #200 (`gh issue view 200`) is about **adk-tracegauge's** README H1 rename, not about tracegauge/token-efficiency-scorer at all — the spec's "check vs. issue #200" pointer for F7 does not actually apply to this finding; there is no live issue tracking the tracegauge naming split itself. |
| F8 | Both | No landed-upstream-OSS section | No "open source" / "upstream" / "merged PR" heading or text found on `/` or `/projects` (`grep -io "open.source\|upstream\|merged pr"` → zero hits) | Same zero hits on README | n/a | **Still open**, and see new finding F19 below — the omission is arguably worse than "just missing," since the one place an upstream claim *is* made (resume.pdf) doesn't hold up under a merge-status check. |
| F9 | GH profile | No pinned repos, "Popular" defaults to gold-rate-tracker | n/a | n/a | `pinnedItems.totalCount: 0` (GraphQL); repos ordered by stargazer count DESC put `gold-rate-tracker` (1 star) first, everything else tied at 0 stars | **Still open.** Verified via `gh api graphql` — zero pins, confirmed stargazer ordering. |
| F10 | GH README vs. site | Research section position | Site `<h2>` order (verified): About me → Experience → **Work** → **Research** → Contact — Research already sits below the projects section | README section order: banner → intro → **Research (2nd section)** → What I work with → **Shipped & live** → How I work → Corrections → Journey → Stack → stats — Research sits above Shipped | n/a | **Still open, README-only.** The site side of this finding is actually already correct/resolved; only the README needs the reorder from spec §3 Phase C. |
| F11 | GH README | "What I work with" / "Stack" duplicate tables | n/a | Two tables, 6 vs. 7 rows, ~80%+ literal overlap on LangGraph/MCP, FAISS/pgvector/BM25/Reranking, CLIP/SBERT/SDXL LoRA/ControlNet/ViT, LLM-as-judge/Conformal/Bootstrap-CUPED, FastAPI/Cloud Run/Vercel/GCP — verified row-by-row, both tables still present, still overlapping | n/a | **Still open.** No change. |
| F12 | Site vs. HF vs. GH issue | Hinglish SBERT model + eval-defect-bench missing from site | Zero hits for "hinglish...sbert", "eval-defect-bench" on `/` or `/projects` (`grep -io`) | n/a | n/a | **Still open.** `hinglish-relatedness-sbert` confirmed live on HF (`curl https://huggingface.co/api/models?author=gauravgandhi2411` → 4 models, includes it). `eval-defect-bench` confirmed as a real, recently-pushed repo (`pushed_at: 2026-09-20`). Issue #122 (`gh issue view 122`) is still **OPEN**, last updated 2026-08-31, tracking exactly this gap plus the profile-README repo itself as an unlisted item. |
| F13 | Site | "14 projects · 12 live" vs. actual grid | Header caption (plain text, no `data-live-value`/citation attached — hardcoded, not provenance-tracked): "14 projects · 12 live." Actual grid: **14** `<article>` project cards confirmed (count matches), but only **9** of the 14 carry a "Live ↗" link (`triageiq, warmer, multimodal-fashion-recommender, style-maitri, aetherart, reclaim, reviewiq, gold-rate-tracker, dealhunter`); the other 5 (`shelfsense, agentgauge, tracegauge, adk-tracegauge, expense-tracker`) have only "Case study" + "Source" links, no "Live" | n/a | n/a | **Still open, now with an exact count.** 14 is correct; "12 live" is wrong by the site's own link-based definition of live — actual is 9, a gap of 3, not the vaguer "shows fewer cards" the spec described. If "live" means something other than "has a Live ↗ link" (e.g. counts HF Spaces without that literal label), that definition isn't stated anywhere on the page — the number is unlabelled either way. |
| F14 | Both | PyPI downloads shown raw | Card downloads-last-week values are plain numbers with no "includes mirrors/CI" caveat text anywhere in the DOM (checked all three package cards) | n/a | n/a | **Still open.** Full mirror/CI analysis already done in §5.4 — pypistats has no official mirror-exclusion endpoint; 74–82% of downloads across all three packages fall in the unattributed `null`-OS bucket, which is a proxy for CI/mirror traffic but not an official classification. Recommendation there stands: label both the raw and OS-attributed figures explicitly, never state either as "real" installs. |
| F15 | gg-portfolio repo | Open bot issues/PRs | n/a | n/a | n/a | **Still open, identical to spec baseline.** `gh issue list --state open`: exactly `#122, #123, #200` (3, matches spec exactly). `gh pr list --state open`: exactly `#99, #124, #125, #132, #135, #137, #152, #206, #208` (9, matches spec exactly). Age check (today 2026-09-23): 7 of the 9 PRs are ≥36 days old (`#152, #137, #135, #132, #125, #124` all created 2026-08-17/18; `#99` created 2026-08-14) — "several 5+ weeks stale" is accurate and, if anything, understated (7 of 9, not "several"). Only `#206` (9 days) and `#208` (2 days) are fresh. |

### 6.2 New findings beyond F1–F15

| # | Surface(s) | Finding | Site value | README value | Bio value | Status |
|---|---|---|---|---|---|---|
| F16 | GH profile vs. resume/README | Contact email mismatch | Resume PDF (p.1, `pdftotext -layout`): `gauravgandhi429@gmail.com` | README footer: `mailto:gauravgandhi429@gmail.com` | GH profile `email` field (`gh api users/gaurav-gandhi-2411 --jq .email`): **`gaurav.gandhi2411@gmail.com`** — a different address entirely | **New, open.** A recruiter using GitHub's own "Email" field on the profile (or an API/scraper reading it) reaches a different inbox than the one on the résumé and README. Low-to-medium severity: not incorrect (2411 is a real, owned address), but it is a second identity for the same person on a page whose whole pitch is "every number sourced" / single consistent identity. |
| F17 | Site vs. resume | Style Maitri catalogue size, precision mismatch | Site (`/projects` embedded JSON, `secondaryMetric.value`): "**52,494** items across 8 stores" — matches README exactly | Resume PDF: "over a **52,000-item** catalogue from 8 stores" (rounded) | n/a | **New, open, minor.** Both are compatible (52,494 rounds to ~52,000) so this isn't a factual contradiction, but it is a precision inconsistency on a page whose stated differentiator is exact, sourced numbers rather than round ones — worth normalizing to 52,494 on the resume for consistency, or explicitly noting the resume rounds for space. |
| F18 | GH profile `company` field vs. site/README | Employer field oversimplified | Site About-me prose: "...my current role inside Uber's AI org (**via Indium Software**)" | README banner: "Uber's AI org (**via Indium Software**)" | GH profile `company` field: **"Uber"** (no mention of Indium) | **New, open, low severity.** Not factually false — Uber is the client org he works inside — but every other surface is careful to name the Indium staffing arrangement explicitly, and the one machine-readable structured field (GitHub's `company`) drops it. Worth a decision on whether this matters for the "one consistent identity" goal or is an acceptable simplification for a single-word profile field. |
| F19 | Resume (site-hosted) vs. reality (google/adk-python) | Unverifiable/likely-false upstream-OSS claim | Resume PDF, under the `tracegauge` bullet: "...**contributed two upstream pull requests to google/adk-python**" | Not mentioned in README (consistent with F8 — no OSS section exists anywhere) | n/a | **New, open, higher severity than F16–F18.** Verified via `gh search prs --author gaurav-gandhi-2411 --repo google/adk-python` (8 total PRs found) + `gh pr view <n> --repo google/adk-python --json mergedAt,state` on each: **0 merged** (`mergedAt: null` on all 6 closed ones), 2 still open (`#6740`, `#6739`), 6 closed-without-merging (`#7000, #6939, #6710, #6682, #6681, #6678`). The resume's phrasing ("contributed... pull requests," presented as a completed accomplishment alongside "reached 87–90% accuracy") reads as landed work; the verifiable state is that nothing from this author has actually merged into `google/adk-python` as of this check. This is exactly the kind of claim the acceptance criteria (spec §4.3: "every listed 'merged' PR confirmed merged via `gh pr view --json mergedAt`") is designed to catch — but it's on the resume, a surface outside spec §4.3's literal scope ("OSS section" on site/README), so it would not be caught by that gate as currently worded. Recommend either updating the resume wording to "submitted" / "under review" or landing at least one of the two open PRs before Phase C ships an "Open source" section that could inherit the same overclaim. |

### 6.3 Summary

- **F1–F15 re-verified against live sources fetched fresh in this session (2026-09-23): all 15 are still open / still conflicting.** None had silently resolved or drifted to a different value since the spec's 2026-09-23 baseline; two (F7, F10) turned out to be **partially resolved on the site side already** (site's tracegauge→token-efficiency-scorer link is correct per PyPI's own metadata, and site's Research section is already positioned below Work) — the outstanding work for both is README-only, which the table above calls out explicitly rather than leaving as a flat "open."
- **4 new findings (F16–F19)** found beyond the spec's checklist, ordered roughly by severity: F19 (resume claims an upstream OSS contribution that doesn't check out as merged — the most substantive) > F13's now-quantified gap (12 claimed vs. 9 actual live projects, not just "fewer") > F16 (GH profile email differs from every other surface) > F17, F18 (minor rounding/field-simplification, arguably acceptable as-is).
- Every value above is labelled with the exact command or URL used to fetch it; nothing here is carried over from the spec's own baseline table without an independent re-check in this session.

File: `C:\Users\gaura\ml-projects\gg-portfolio-wt-refresh-audit\docs\refresh-2026-09\audit-parts\06-cross-surface-consistency.md`
