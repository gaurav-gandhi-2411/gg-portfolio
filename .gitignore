# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# raw resume drops (canonical copy lives at public/resume.pdf, tracked)
.assets/

# Playwright (wave 14) — generated per-run, never source
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Local scratch cache for offline data-build scripts (e.g. downloaded source
# images for scripts/build_mmfr_projection.py) — large, regenerable, never
# committed. The output JSON these scripts write under content/data/ IS
# committed; this cache is not that.
/.cache/

# Claude Code session/scheduler state — not project content (but .claude/settings.json,
# if added, is project config and should stay tracked)
.claude/*.lock
.claude/scheduled_tasks*
